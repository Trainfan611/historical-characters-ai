# ✨ Новые возможности: Гибкая база исторических личностей

## 🚀 Что изменилось

База данных теперь **динамически расширяется** через поиск в интернете!

### Основные улучшения:

1. **Поиск через интернет** 🌐
   - Система автоматически ищет информацию о любой исторической личности через Perplexity API
   - Не ограничивается только предустановленными 15 личностями
   - Работает с любым именем исторической личности

2. **Автоматическое кэширование** 💾
   - Найденные через интернет личности автоматически сохраняются в БД
   - При повторном поиске используются данные из БД (быстрее и дешевле)
   - База данных постоянно растет

3. **Улучшенный поиск** 🔍
   - Поиск работает как в БД, так и в интернете одновременно
   - Результаты объединяются и сортируются
   - Поддержка частичного совпадения имен

4. **Умное извлечение данных** 🧠
   - Автоматическое определение эпохи
   - Извлечение годов рождения и смерти
   - Определение страны и категории
   - Описание внешности из исторических источников

## 📝 Как это работает

### При генерации изображения:

1. Пользователь вводит имя (например, "Владимир Ленин")
2. Система сначала ищет в локальной БД
3. Если не находит - ищет через Perplexity API (интернет)
4. Извлекает информацию: биографию, эпоху, внешность, годы жизни
5. Сохраняет найденную личность в БД для будущего использования
6. Генерирует изображение на основе найденной информации

### При поиске:

1. Пользователь вводит запрос в поисковую строку
2. Система ищет в БД и показывает результаты
3. Если результатов мало - дополнительно ищет в интернете
4. Объединяет результаты и показывает пользователю
5. Сохраняет новые личности в БД

## 🎯 Примеры использования

Теперь можно генерировать изображения для:

- ✅ **Любых исторических личностей** (не только из предустановленного списка)
- ✅ **Разных эпох** (от древности до современности)
- ✅ **Разных стран и культур**
- ✅ **Разных категорий** (политики, ученые, художники, военные и т.д.)

### Примеры имен для тестирования:

- Владимир Ленин
- Иосиф Сталин
- Петр I
- Екатерина Великая
- Чарльз Дарвин
- Мария Кюри
- Винсент ван Гог
- Фридрих Ницше
- Конфуций
- Аристотель
- И многие другие!

## 🔧 Технические детали

### Новые функции:

- `findOrCreatePerson()` - поиск в БД или создание через интернет
- `searchPersons()` - умный поиск с поддержкой интернета
- `searchPersonsWithAutoExpand()` - автоматическое расширение базы

### API endpoints:

- `GET /api/persons?q=name&useInternet=true` - поиск с интернетом
- `GET /api/persons/search?q=name` - расширенный поиск
- `POST /api/generate` - автоматически использует интернет-поиск если не находит в БД

## 💡 Преимущества

1. **Неограниченная база** - работа с любыми историческими личностями
2. **Автоматическое обучение** - база растет с каждым поиском
3. **Быстрый доступ** - кэширование найденных личностей
4. **Точность** - использование актуальных данных из интернета
5. **Гибкость** - поддержка разных форматов имен и языков

## 📊 Статистика

- Начальная база: 15 личностей
- После использования: неограниченно (растет автоматически)
- Источник данных: Perplexity AI (поиск в интернете)
- Кэширование: автоматическое в PostgreSQL

---

**Теперь ваша платформа может работать с любой исторической личностью! 🎉**
