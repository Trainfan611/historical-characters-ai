# 🚨 СРОЧНО: Исправление ошибки isAdmin

## Проблема
```
The column `User.isAdmin` does not exist in the current database
```

## ✅ БЫСТРОЕ РЕШЕНИЕ (выберите один способ)

### Способ 1: Через Railway PostgreSQL (САМЫЙ БЫСТРЫЙ - 30 секунд)

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект
3. Найдите сервис **PostgreSQL** (база данных)
4. Нажмите на него
5. Перейдите в раздел **"Query"** или **"Data"**
6. Скопируйте и вставьте этот SQL:

```sql
ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "isAdmin" BOOLEAN NOT NULL DEFAULT false;
CREATE INDEX IF NOT EXISTS "User_isAdmin_idx" ON "User"("isAdmin");
```

7. Нажмите **"Run"** или **"Execute"**
8. ✅ Готово! Ошибка исчезнет

---

### Способ 2: Через Railway Shell

1. Railway Dashboard → ваш проект → Deployments → последний deployment
2. Откройте **"Shell"**
3. Выполните:

```bash
npx prisma db push
```

---

### Способ 3: Через Railway CLI

```bash
railway run npx prisma db push
```

---

## ✅ Проверка

После выполнения SQL, проверьте что колонка добавлена:

```sql
SELECT column_name, data_type, column_default 
FROM information_schema.columns 
WHERE table_name = 'User' AND column_name = 'isAdmin';
```

Должно вернуть строку с `isAdmin`.

---

## 🎯 После исправления

1. Ошибка исчезнет автоматически
2. Перезапустите приложение (если нужно)
3. Откройте: `https://ваш-домен.railway.app/admin/setup`
4. Нажмите **"Назначить меня админом"**

---

**Это займет 30 секунд!** ⚡
