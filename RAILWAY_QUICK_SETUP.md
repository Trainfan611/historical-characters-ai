# ⚡ Быстрая настройка на Railway (2 команды)

## 🎯 Вариант 1: Через Railway Dashboard (Самый простой)

### Шаг 1: Применить миграции
1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект
3. Перейдите в **"Deployments"** → последний deployment
4. Откройте **"Shell"** или **"Logs"**
5. Выполните команду:
   ```bash
   npm run railway:setup
   ```
   Или напрямую:
   ```bash
   npm run db:deploy
   ```

### Шаг 2: Назначить админа
1. Откройте ваше приложение: `https://ваш-домен.railway.app`
2. Войдите через Telegram
3. Перейдите на: `https://ваш-домен.railway.app/admin/setup`
4. Нажмите **"Назначить меня админом"**

---

## 🎯 Вариант 2: Через Railway CLI

Если у вас установлен Railway CLI:

```bash
# Шаг 1: Применить миграции
railway run npm run railway:setup

# Шаг 2: Назначить админа (через браузер)
# Откройте https://ваш-домен.railway.app/admin/setup
```

---

## 🎯 Вариант 3: Автоматически при деплое

Добавьте в Railway переменную окружения:

**Name:** `RAILWAY_RUN_COMMAND`  
**Value:** `npm run railway:setup && npm start`

Это автоматически применит миграции при каждом деплое.

---

## ✅ Проверка

После выполнения обоих шагов:
- Откройте `/admin` - должна открыться админ-панель
- Если видите ошибку доступа, вернитесь к шагу 2

---

## 🆘 Если что-то пошло не так

### Миграции не применяются
```bash
# Попробуйте напрямую
railway run npx prisma db push
```

### Не могу назначить админа
1. Убедитесь, что вы вошли через Telegram
2. Проверьте, что миграции применены (поле `isAdmin` существует в БД)
3. Попробуйте через API:
   ```bash
   curl -X POST https://ваш-домен.railway.app/api/admin/make-me-admin \
     -H "Cookie: next-auth.session-token=ваш_токен"
   ```

---

**Готово! Теперь у вас есть админ-панель!** 🎉
