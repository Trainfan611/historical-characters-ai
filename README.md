# 🎨 Historical Characters AI

Веб-платформа для генерации изображений исторических личностей с помощью AI.

## ✨ Функции

- 🔐 Регистрация через Telegram
- ✅ Обязательная подписка на Telegram канал
- 🤖 Генерация изображений через AI (Replicate/Flux)
- 📚 **Гибкая база исторических личностей** - поиск через интернет для любой личности
- 🌐 Автоматическое расширение базы через Perplexity API
- 📊 История генераций

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/historical_characters"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"
TELEGRAM_BOT_TOKEN="your-bot-token"
TELEGRAM_CHANNEL_ID="@your-channel"
OPENROUTER_API_KEY="your-key"
PERPLEXITY_API_KEY="your-key"
OPENAI_API_KEY="your-key"
```

### 3. Настройка базы данных

```bash
# Создать миграции
npm run db:push

# Заполнить базу тестовыми данными
npm run db:seed
```

### 4. Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📋 Настройка Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите `TELEGRAM_BOT_TOKEN`
3. Создайте публичный канал
4. Добавьте бота как администратора канала
5. Укажите `TELEGRAM_CHANNEL_ID` в `.env` (формат: `@channelname` или `-1001234567890`)
6. Для Telegram Login Widget укажите `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` в `.env`

## 🛠️ Технологии

- Next.js 14 + TypeScript
- Prisma + PostgreSQL
- NextAuth.js
- Tailwind CSS
- Replicate API (Flux) - генерация изображений
- Perplexity API - поиск информации о личностях в интернете
- OpenAI API - формирование промптов

## 📁 Структура проекта

```
├── app/              # Next.js App Router
│   ├── api/         # API endpoints
│   ├── generate/    # Страница генерации
│   └── profile/     # Профиль пользователя
├── components/      # React компоненты
├── lib/             # Утилиты и хелперы
│   ├── ai/         # AI интеграции
│   └── telegram.ts # Telegram API
└── prisma/          # Prisma схема
```

## 📝 Скрипты

- `npm run dev` - Запуск dev сервера
- `npm run build` - Production сборка
- `npm run start` - Запуск production сервера
- `npm run db:push` - Применить изменения схемы БД
- `npm run db:seed` - Заполнить базу тестовыми данными
- `npm run db:studio` - Открыть Prisma Studio

## 🔧 Требования

- Node.js 18+
- PostgreSQL 14+
- Telegram Bot Token
- API ключи: OpenRouter, Perplexity, OpenAI

## 📄 Лицензия

MIT
