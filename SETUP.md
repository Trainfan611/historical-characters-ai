# 🚀 Инструкция по настройке проекта

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка базы данных

1. Установите PostgreSQL (если еще не установлен)
2. Создайте базу данных:
```sql
CREATE DATABASE historical_characters;
```

3. Обновите `DATABASE_URL` в `.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/historical_characters"
```

4. Примените схему и заполните тестовыми данными:
```bash
npm run db:push
npm run db:seed
```

## Шаг 3: Настройка Telegram

### Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный `BOT_TOKEN`

### Создание канала

1. Создайте публичный канал в Telegram
2. Добавьте бота как администратора канала:
   - Откройте настройки канала
   - Администраторы → Добавить администратора
   - Найдите вашего бота и добавьте его
3. Получите ID канала:
   - Для публичного канала: используйте `@channelname`
   - Для приватного: используйте бота [@userinfobot](https://t.me/userinfobot) чтобы получить ID (формат: `-1001234567890`)

### Настройка Telegram Login Widget

1. В [@BotFather](https://t.me/BotFather) настройте домен для виджета:
   - `/setdomain` → выберите бота → укажите ваш домен (для localhost используйте `localhost`)
2. Укажите `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` в `.env` (имя бота без @)

## Шаг 4: Получение API ключей

### Replicate API (для генерации изображений)

1. Зарегистрируйтесь на [replicate.com](https://replicate.com)
2. Перейдите в [Account Settings → API Tokens](https://replicate.com/account/api-tokens)
3. Создайте новый токен
4. Добавьте в `.env`: `REPLICATE_API_KEY="r8_..."`

**Альтернатива:** Можно использовать другие сервисы для генерации изображений:
- Stability AI
- Midjourney API
- DALL-E API

### Perplexity API

1. Зарегистрируйтесь на [perplexity.ai](https://www.perplexity.ai)
2. Перейдите в API Settings
3. Создайте API ключ
4. Добавьте в `.env`: `PERPLEXITY_API_KEY="pplx-..."`

### OpenAI API

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в API Keys
3. Создайте новый ключ
4. Добавьте в `.env`: `OPENAI_API_KEY="sk-..."`

## Шаг 5: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/historical_characters"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-random-string-here"

# Telegram
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
TELEGRAM_CHANNEL_ID="@your-channel"
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="your_bot_username"

# AI APIs
REPLICATE_API_KEY="r8_..."
PERPLEXITY_API_KEY="pplx-..."
OPENAI_API_KEY="sk-..."
```

**Важно:** 
- `NEXTAUTH_SECRET` - сгенерируйте случайную строку (можно использовать `openssl rand -base64 32`)
- Для production измените `NEXTAUTH_URL` на ваш домен

## Шаг 6: Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🐛 Решение проблем

### Ошибка подключения к базе данных

- Проверьте, что PostgreSQL запущен
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что база данных создана

### Ошибка Telegram авторизации

- Проверьте `TELEGRAM_BOT_TOKEN`
- Убедитесь, что домен настроен в BotFather
- Проверьте `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME`

### Ошибка проверки подписки

- Убедитесь, что бот добавлен как администратор канала
- Проверьте правильность `TELEGRAM_CHANNEL_ID`
- Для приватных каналов используйте числовой ID

### Ошибка генерации изображений

- Проверьте `REPLICATE_API_KEY`
- Убедитесь, что на счету Replicate есть средства
- Проверьте логи в консоли сервера

## 📝 Дополнительные команды

```bash
# Открыть Prisma Studio (GUI для БД)
npm run db:studio

# Создать новую миграцию
npm run db:migrate

# Сбросить базу данных (осторожно!)
npx prisma migrate reset
```

## 🚀 Production деплой

1. Настройте production переменные окружения
2. Соберите проект: `npm run build`
3. Запустите: `npm start`
4. Настройте reverse proxy (Nginx/Caddy)
5. Настройте SSL сертификаты
