# 📊 Админ-панель - Руководство

## 🎯 Возможности

Админ-панель позволяет отслеживать:
- ✅ Всех пользователей, которые подписались на канал и прошли через бота
- ✅ Активность пользователей (количество запросов, генераций)
- ✅ Статистику по дням/часам
- ✅ Топ пользователей по активности
- ✅ Популярные исторические личности
- ✅ Статистику подписок

## 🚀 Быстрый старт

### 1. Обновление базы данных

Сначала нужно обновить схему базы данных, чтобы добавить поле `isAdmin`:

```bash
npx prisma db push
```

Или через миграцию:

```bash
npx prisma migrate dev --name add_admin_field
```

### 2. Назначение админа

Чтобы получить доступ к админ-панели, нужно назначить себя админом.

**Способ 1: Через скрипт (рекомендуется)**

```bash
npm run admin:make <ваш_telegram_id>
```

Например:
```bash
npm run admin:make 123456789
```

**Способ 2: Через Prisma Studio**

```bash
npm run db:studio
```

1. Откройте таблицу `User`
2. Найдите свою запись по `telegramId`
3. Установите `isAdmin` в `true`
4. Сохраните изменения

**Способ 3: Через SQL**

```sql
UPDATE "User" SET "isAdmin" = true WHERE "telegramId" = 'ваш_telegram_id';
```

### 3. Доступ к админ-панели

1. Войдите в приложение через Telegram
2. Перейдите по адресу: `https://ваш-домен.com/admin`
3. Если вы админ, откроется панель управления

## 📊 Разделы админ-панели

### 1. Обзор (Overview)

**Основные метрики:**
- Всего пользователей
- Подписанных пользователей
- Всего генераций
- Успешных генераций

**Дополнительная информация:**
- Топ 10 пользователей по генерациям
- Популярные исторические личности
- Статистика по дням (последние 30 дней)

### 2. Пользователи (Users)

**Возможности:**
- Просмотр всех пользователей
- Поиск по имени, username, Telegram ID
- Фильтрация по статусу подписки
- Статистика для каждого пользователя:
  - Всего генераций
  - Генераций сегодня
  - Дата регистрации
  - Последняя активность

### 3. Активность (Activity)

**Информация:**
- Активность по часам (последние 7 дней)
- Количество генераций в каждый час
- Уникальные пользователи в каждый период
- Топ 20 самых активных пользователей

## 🔐 Безопасность

- Админ-панель доступна только пользователям с `isAdmin = true`
- Все API endpoints проверяют права доступа
- Неавторизованные запросы возвращают 403 Forbidden

## 📡 API Endpoints

### GET /api/admin/stats
Получает общую статистику.

**Параметры:**
- `days` (query) - количество дней для статистики (по умолчанию 30)

**Пример:**
```bash
GET /api/admin/stats?days=7
```

### GET /api/admin/users
Получает список пользователей.

**Параметры:**
- `page` (query) - номер страницы (по умолчанию 1)
- `limit` (query) - количество на странице (по умолчанию 50)
- `search` (query) - поисковый запрос
- `subscribed` (query) - фильтр по подписке: `true`, `false`, `all`
- `sortBy` (query) - поле для сортировки: `createdAt`, `generations`
- `sortOrder` (query) - порядок сортировки: `asc`, `desc`

**Пример:**
```bash
GET /api/admin/users?page=1&limit=50&search=john&subscribed=true
```

### GET /api/admin/activity
Получает активность пользователей.

**Параметры:**
- `days` (query) - количество дней (по умолчанию 7)
- `groupBy` (query) - группировка: `hour`, `day` (по умолчанию `hour`)

**Пример:**
```bash
GET /api/admin/activity?days=7&groupBy=hour
```

## 💡 Полезные советы

### Экспорт данных

Вы можете экспортировать данные через API endpoints:

```bash
# Получить всех пользователей
curl https://ваш-домен.com/api/admin/users?limit=1000 \
  -H "Cookie: next-auth.session-token=ваш_токен"

# Получить статистику
curl https://ваш-домен.com/api/admin/stats?days=365 \
  -H "Cookie: next-auth.session-token=ваш_токен"
```

### Мониторинг активности

Рекомендуется регулярно проверять:
- Количество новых пользователей
- Процент подписок
- Успешность генераций
- Топ активных пользователей

### Анализ данных

Используйте данные для:
- Понимания поведения пользователей
- Оптимизации лимитов генераций
- Выявления проблем
- Планирования масштабирования

## 🐛 Решение проблем

### "У вас нет прав доступа к админ-панели"

**Решение:**
1. Убедитесь, что вы назначили себя админом (см. раздел "Назначение админа")
2. Выйдите и войдите снова в приложение
3. Проверьте, что `isAdmin = true` в базе данных

### "Failed to fetch stats"

**Решение:**
1. Проверьте подключение к базе данных
2. Убедитесь, что миграции применены
3. Проверьте логи сервера

### Данные не обновляются

**Решение:**
1. Обновите страницу (F5)
2. Проверьте, что API endpoints возвращают данные
3. Проверьте консоль браузера на ошибки

## 📝 Примеры использования

### Найти всех не подписанных пользователей

```bash
GET /api/admin/users?subscribed=false&limit=1000
```

### Получить статистику за последний месяц

```bash
GET /api/admin/stats?days=30
```

### Найти пользователя по Telegram ID

```bash
GET /api/admin/users?search=123456789
```

## 🔄 Обновление базы данных

Если нужно обновить схему базы данных:

```bash
# Применить изменения
npx prisma db push

# Или создать миграцию
npx prisma migrate dev --name название_миграции
```

---

**Готово! Теперь вы можете отслеживать всех пользователей и их активность.** 🎉
