# 🏗️ Архитектура проекта

## 📐 Общая архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                        CLIENT (Browser)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Next.js    │  │   React      │  │  Tailwind    │     │
│  │   Frontend   │  │  Components  │  │     CSS      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTPS
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    NEXT.JS SERVER (VPS)                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Next.js API Routes                       │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │  │
│  │  │   /api/auth  │  │ /api/generate│  │ /api/...  │ │  │
│  │  └──────────────┘  └──────────────┘  └───────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              NextAuth.js                              │  │
│  │  ┌──────────────┐  ┌──────────────┐                 │  │
│  │  │  Telegram    │  │  JWT Tokens  │                 │  │
│  │  │  Provider    │  │              │                 │  │
│  │  └──────────────┘  └──────────────┘                 │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  PostgreSQL  │  │  Telegram    │  │  AI Services │
│   Database   │  │  Bot API     │  │              │
│              │  │              │  │  ┌─────────┐ │
│  - Users     │  │  - Auth      │  │  │OpenRouter│ │
│  - Generations│ │  - Subscription│ │  │Perplexity│ │
│  - Persons   │  │              │  │  │  OpenAI │ │
└──────────────┘  └──────────────┘  └──┴─────────┘─┘
```

---

## 🔄 Flow генерации изображения

```
User → Select Person → Click Generate
  │
  ▼
┌─────────────────────────────────────┐
│  1. Check Subscription Status       │
│     (Telegram Bot API)              │
└─────────────────────────────────────┘
  │
  ├─ Not Subscribed → Show Subscribe Button
  │
  └─ Subscribed → Continue
      │
      ▼
┌─────────────────────────────────────┐
│  2. Search Person Info              │
│     (Perplexity API)                │
│     → Get: appearance, era, style   │
└─────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────┐
│  3. Generate Prompt                 │
│     (OpenAI GPT-4)                  │
│     → Create detailed image prompt  │
└─────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────┐
│  4. Generate Image                  │
│     (OpenRouter - Flux)             │
│     → Create image from prompt      │
└─────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────┐
│  5. Save to Database                │
│     (Prisma → PostgreSQL)           │
│     → Store: imageUrl, prompt, etc  │
└─────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────┐
│  6. Return Result to User           │
│     → Display image                 │
│     → Show download/share options   │
└─────────────────────────────────────┘
```

---

## 🗄️ Схема базы данных

```
┌─────────────────────────────────────────────────────────┐
│                      User                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ id          │  │ telegramId  │  │ username    │    │
│  │ firstName   │  │ isSubscribed│  │ createdAt   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────────────┘
         │                              │
         │                              │
         │ 1:N                          │ 1:N
         │                              │
         ▼                              ▼
┌─────────────────────┐    ┌─────────────────────────────┐
│   Generation        │    │   SubscriptionCheck         │
│  ┌───────────────┐  │    │  ┌──────────────┐          │
│  │ id            │  │    │  │ id           │          │
│  │ userId        │  │    │  │ userId       │          │
│  │ personName    │  │    │  │ channelId    │          │
│  │ prompt        │  │    │  │ isSubscribed │          │
│  │ imageUrl      │  │    │  │ lastChecked │          │
│  │ createdAt     │  │    │  └──────────────┘          │
│  └───────────────┘  │    └─────────────────────────────┘
└─────────────────────┘
         │
         │ N:1
         │
         ▼
┌─────────────────────────────────────┐
│   HistoricalPerson                  │
│  ┌─────────────┐  ┌─────────────┐  │
│  │ id          │  │ name        │  │
│  │ nameEn      │  │ description │  │
│  │ era         │  │ category    │  │
│  │ country     │  │ birthYear   │  │
│  └─────────────┘  └─────────────┘  │
└─────────────────────────────────────┘
```

---

## 🔐 Flow аутентификации

```
User clicks "Login with Telegram"
  │
  ▼
┌─────────────────────────────────────┐
│  Telegram Login Widget              │
│  (Official Telegram Widget)         │
└─────────────────────────────────────┘
  │
  │ User authorizes
  │
  ▼
┌─────────────────────────────────────┐
│  Send data to /api/auth/callback     │
│  - id, hash, username, first_name    │
└─────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────┐
│  Verify Telegram Data                │
│  (Check hash validity)               │
└─────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────┐
│  Create/Update User in DB            │
│  (Prisma)                            │
└─────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────┐
│  Create JWT Session                  │
│  (NextAuth.js)                       │
└─────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────┐
│  Redirect to Home                    │
│  User is authenticated              │
└─────────────────────────────────────┘
```

---

## ✅ Flow проверки подписки

```
User tries to access /generate
  │
  ▼
┌─────────────────────────────────────┐
│  Middleware checks subscription     │
│  (Next.js Middleware)               │
└─────────────────────────────────────┘
  │
  ├─ Check DB cache (lastChecked < 24h)
  │  │
  │  ├─ Cached & Subscribed → Allow
  │  │
  │  └─ Cached & Not Subscribed → Block
  │
  └─ Not cached or expired
      │
      ▼
┌─────────────────────────────────────┐
│  Call Telegram Bot API               │
│  getChatMember(userId, channelId)   │
└─────────────────────────────────────┘
      │
      ├─ Status: "member" or "administrator"
      │  │
      │  ▼
      │  ┌─────────────────────────────┐
      │  │ Update DB: isSubscribed=true│
      │  │ Allow access                │
      │  └─────────────────────────────┘
      │
      └─ Status: "left" or "kicked"
          │
          ▼
      ┌─────────────────────────────┐
      │ Update DB: isSubscribed=false│
      │ Block access                 │
      │ Show subscribe button        │
      └─────────────────────────────┘
```

---

## 📁 Структура файлов

```
historical-characters-ai/
│
├── app/                          # Next.js App Router
│   ├── (auth)/
│   │   ├── login/               # Страница входа
│   │   └── subscribe/           # Страница подписки
│   │
│   ├── api/
│   │   ├── auth/
│   │   │   └── [...nextauth]/   # NextAuth endpoint
│   │   ├── generate/            # Генерация изображений
│   │   ├── persons/             # Исторические личности
│   │   ├── generations/         # История генераций
│   │   └── subscription/        # Проверка подписки
│   │
│   ├── generate/                # Страница генерации
│   ├── profile/                 # Профиль пользователя
│   ├── layout.tsx               # Root layout
│   └── page.tsx                 # Главная страница
│
├── components/
│   ├── ui/                      # Базовые UI компоненты
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   └── Modal.tsx
│   │
│   ├── auth/
│   │   ├── TelegramLogin.tsx    # Telegram Login Widget
│   │   └── SubscriptionCheck.tsx
│   │
│   ├── generation/
│   │   ├── PersonSearch.tsx
│   │   ├── GenerationForm.tsx
│   │   └── GenerationResult.tsx
│   │
│   └── layout/
│       ├── Header.tsx
│       └── Footer.tsx
│
├── lib/
│   ├── db.ts                    # Prisma client
│   ├── auth.ts                  # NextAuth config
│   │
│   ├── telegram.ts              # Telegram API
│   │   ├── verifyTelegramData()
│   │   └── checkSubscription()
│   │
│   └── ai/
│       ├── perplexity.ts        # Perplexity API
│       ├── openai.ts            # OpenAI API
│       └── openrouter.ts        # OpenRouter API
│
├── prisma/
│   ├── schema.prisma            # Prisma схема
│   └── seed.ts                  # Seed данные
│
├── public/                      # Статические файлы
│
├── .env.example                 # Пример переменных окружения
├── .gitignore
├── package.json
├── tsconfig.json
└── tailwind.config.ts
```

---

## 🔌 API Endpoints

### Аутентификация
- `GET /api/auth/signin` - Страница входа
- `POST /api/auth/callback/telegram` - Callback от Telegram
- `GET /api/auth/session` - Получить текущую сессию
- `POST /api/auth/signout` - Выход

### Генерация
- `POST /api/generate` - Генерация изображения
  - Body: `{ personId: string, style?: string }`
  - Response: `{ imageUrl: string, generationId: string }`

### Исторические личности
- `GET /api/persons` - Список всех личностей
- `GET /api/persons/search?q=name` - Поиск по имени
- `GET /api/persons/:id` - Детали личности

### Генерации
- `GET /api/generations` - История генераций пользователя
- `GET /api/generations/:id` - Детали генерации
- `DELETE /api/generations/:id` - Удалить генерацию

### Подписка
- `GET /api/subscription/status` - Статус подписки
- `POST /api/subscription/check` - Принудительная проверка

---

## 🛡️ Безопасность

### Защита API
- ✅ JWT токены для аутентификации
- ✅ Rate limiting на все endpoints
- ✅ Валидация всех входных данных (Zod)
- ✅ Проверка подписки перед генерацией

### Защита данных
- ✅ HTTPS только
- ✅ Безопасное хранение API ключей (.env)
- ✅ SQL injection защита (Prisma)
- ✅ CSRF защита (NextAuth.js)

### Telegram
- ✅ Верификация hash от Telegram
- ✅ Проверка подписки через официальный API
- ✅ Периодическое обновление статуса подписки

---

## 📊 Производительность

### Оптимизации
- ✅ Кэширование промптов для популярных личностей
- ✅ Database индексы на часто используемые поля
- ✅ Lazy loading изображений
- ✅ Next.js Image оптимизация
- ✅ API response кэширование

### Мониторинг
- ✅ Логирование всех API запросов
- ✅ Отслеживание ошибок
- ✅ Мониторинг использования API (квоты)
- ✅ Database query оптимизация

---

## 🚀 Деплой

### Production Stack
```
Internet
  │
  ▼
Cloudflare (CDN, DDoS protection)
  │
  ▼
Caddy (Reverse Proxy, SSL)
  │
  ▼
Next.js App (PM2)
  │
  ├─→ PostgreSQL Database
  └─→ External APIs (Telegram, AI services)
```

### Environment Variables
- Production `.env` файл с реальными ключами
- Отдельные ключи для production и development
- Безопасное хранение секретов

---

## 📈 Масштабирование

### Горизонтальное масштабирование
- Load balancer перед несколькими Next.js инстансами
- Database connection pooling
- Redis для кэширования сессий

### Вертикальное масштабирование
- Увеличение ресурсов VPS
- Оптимизация database запросов
- CDN для статических файлов

---

**Архитектура спроектирована для масштабируемости и надежности! 🚀**
