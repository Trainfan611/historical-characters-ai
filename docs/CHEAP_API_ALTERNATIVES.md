# 💰 Дешёвые альтернативы API для вашего проекта

## 📊 Текущие API и их стоимость

### Текущая конфигурация:
- **Perplexity API** - поиск информации (~$0.001/запрос)
- **OpenAI API** - генерация промптов (~$0.01/запрос)
- **Replicate API** - генерация изображений (~$0.01/запрос)

**Итого для 16000 запросов/день:** ~$336/день

---

## 1. Альтернативы для поиска информации (вместо Perplexity)

### 1.1. **SerpAPI** (Google Search API)
- **Цена:** $50/месяц за 5000 запросов = **$0.01/запрос**
- **Преимущества:** Реальные результаты Google, очень точные
- **Недостатки:** Дороже Perplexity
- **Сайт:** https://serpapi.com/

### 1.2. **Serper.dev** (Google Search API)
- **Цена:** $50/месяц за 2500 запросов = **$0.02/запрос**
- **Преимущества:** Быстрый, простой API
- **Сайт:** https://serper.dev/

### 1.3. **Tavily AI** (AI Search)
- **Цена:** $99/месяц за 1000 запросов = **$0.099/запрос**
- **Преимущества:** Специально для AI, хорошее качество
- **Сайт:** https://tavily.com/

### 1.4. **Google Custom Search API** (бесплатный tier)
- **Цена:** **БЕСПЛАТНО** до 100 запросов/день, затем $5 за 1000 запросов = **$0.005/запрос**
- **Преимущества:** Очень дёшево, официальный Google API
- **Недостатки:** Ограниченный бесплатный tier
- **Сайт:** https://developers.google.com/custom-search

### 1.5. **Bing Web Search API** (Microsoft)
- **Цена:** **БЕСПЛАТНО** до 1000 запросов/месяц, затем $4 за 1000 запросов = **$0.004/запрос**
- **Преимущества:** Очень дёшево, хорошее качество
- **Сайт:** https://www.microsoft.com/en-us/bing/apis/bing-web-search-api

**🏆 Рекомендация:** **Bing Web Search API** - самый дешёвый вариант ($0.004/запрос)

---

## 2. Альтернативы для генерации промптов (вместо OpenAI)

### 2.1. **Anthropic Claude API** (Claude 3 Haiku)
- **Цена:** $0.25 за 1M input tokens, $1.25 за 1M output tokens
- **Для промпта (~300 токенов):** ~**$0.0004/запрос**
- **Преимущества:** Дешевле GPT-4, хорошее качество
- **Сайт:** https://www.anthropic.com/pricing

### 2.2. **Google Gemini API** (Gemini Pro)
- **Цена:** **БЕСПЛАТНО** до 15 RPM, затем $0.0005 за 1K tokens
- **Для промпта (~300 токенов):** ~**$0.00015/запрос** (почти бесплатно!)
- **Преимущества:** Очень дёшево, хорошее качество
- **Сайт:** https://ai.google.dev/pricing

### 2.3. **Groq API** (Llama 3, Mixtral)
- **Цена:** **БЕСПЛАТНО** до 30 RPM, затем очень дёшево
- **Для промпта:** ~**$0.0001/запрос** (почти бесплатно!)
- **Преимущества:** Очень быстро, очень дёшево
- **Сайт:** https://groq.com/pricing

### 2.4. **Together AI**
- **Цена:** $0.0002 за 1K tokens
- **Для промпта (~300 токенов):** ~**$0.00006/запрос**
- **Преимущества:** Очень дёшево, много моделей
- **Сайт:** https://www.together.ai/pricing

### 2.5. **OpenRouter** (агрегатор моделей)
- **Цена:** Зависит от модели, но есть дешёвые варианты
- **Для промпта:** от **$0.0001/запрос**
- **Преимущества:** Доступ ко многим моделям, можно выбрать самую дешёвую
- **Сайт:** https://openrouter.ai/models

**🏆 Рекомендация:** **Google Gemini API** или **Groq API** - почти бесплатно!

---

## 3. Альтернативы для генерации изображений (вместо Replicate)

### 3.1. **Stable Diffusion API** (Stability AI)
- **Цена:** $0.04 за изображение
- **Преимущества:** Дешевле Replicate
- **Сайт:** https://platform.stability.ai/pricing

### 3.2. **Flux API** (Black Forest Labs)
- **Цена:** $0.05 за изображение
- **Преимущества:** Лучшее качество, но дороже
- **Сайт:** https://blackforestlabs.ai/pricing

### 3.3. **Fal.ai** (Flux, SDXL)
- **Цена:** $0.003-0.01 за изображение (зависит от модели)
- **Преимущества:** Очень дёшево, быстрая генерация
- **Сайт:** https://fal.ai/pricing

### 3.4. **Replicate** (текущий, но можно выбрать дешёвую модель)
- **Цена:** Зависит от модели
  - `flux-dev`: $0.003 за изображение
  - `flux-schnell`: $0.002 за изображение
- **Преимущества:** Уже используете, просто выберите дешёвую модель
- **Сайт:** https://replicate.com/pricing

### 3.5. **Hugging Face Inference API**
- **Цена:** **БЕСПЛАТНО** для некоторых моделей, затем $0.002-0.01 за изображение
- **Преимущества:** Бесплатный tier доступен
- **Сайт:** https://huggingface.co/pricing

### 3.6. **Novita AI**
- **Цена:** $0.001-0.003 за изображение
- **Преимущества:** Очень дёшево, хорошее качество
- **Сайт:** https://novita.ai/pricing

**🏆 Рекомендация:** **Fal.ai** или **Replicate flux-schnell** - самые дешёвые ($0.002-0.003/изображение)

---

## 4. Оптимальная конфигурация (самая дешёвая)

### Вариант 1: Максимальная экономия
- **Поиск:** Bing Web Search API - **$0.004/запрос**
- **Промпт:** Google Gemini API - **$0.00015/запрос**
- **Изображение:** Fal.ai или Replicate flux-schnell - **$0.002/запрос**

**Итого для 16000 запросов/день:** 
- Поиск: 16000 × $0.004 = **$64/день**
- Промпт: 16000 × $0.00015 = **$2.4/день**
- Изображение: 16000 × $0.002 = **$32/день**

**Всего: ~$98.4/день** (экономия ~$237/день по сравнению с текущей конфигурацией!)

### Вариант 2: Баланс цена/качество
- **Поиск:** Perplexity API - **$0.001/запрос** (оставить текущий)
- **Промпт:** Google Gemini API - **$0.00015/запрос**
- **Изображение:** Fal.ai - **$0.002/запрос**

**Итого:** ~$50/день (экономия ~$286/день!)

---

## 5. Как переключиться на дешёвые API

### 5.1. Переключение на Bing Web Search API

```typescript
// lib/ai/bing-search.ts
import axios from 'axios';

const BING_API_KEY = process.env.BING_SEARCH_API_KEY;
const BING_API_URL = 'https://api.bing.microsoft.com/v7.0/search';

export async function searchHistoricalPerson(personName: string) {
  try {
    const response = await axios.get(BING_API_URL, {
      params: {
        q: `historical person ${personName} biography appearance`,
        count: 5,
        mkt: 'en-US',
      },
      headers: {
        'Ocp-Apim-Subscription-Key': BING_API_KEY,
      },
    });
    
    // Парсим результаты и формируем PersonInfo
    // ...
  } catch (error) {
    console.error('Bing search error:', error);
    return null;
  }
}
```

### 5.2. Переключение на Google Gemini API

```typescript
// lib/ai/gemini.ts
import { GoogleGenerativeAI } from '@google/generative-ai';

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY!);

export async function generateImagePrompt(
  personInfo: PersonInfo,
  style: string = 'realistic'
): Promise<string> {
  const model = genAI.getGenerativeModel({ model: 'gemini-pro' });
  
  const prompt = `Create a detailed image generation prompt for ${personInfo.name}...`;
  
  const result = await model.generateContent(prompt);
  const response = await result.response;
  return response.text();
}
```

### 5.3. Переключение на Fal.ai для изображений

```typescript
// lib/ai/fal.ts
import axios from 'axios';

const FAL_API_KEY = process.env.FAL_API_KEY;
const FAL_API_URL = 'https://fal.run/fal-ai/flux/schnell';

export async function generateImage(prompt: string): Promise<string> {
  try {
    // Создаём задачу
    const createResponse = await axios.post(
      FAL_API_URL,
      {
        prompt,
        image_size: 'square',
        num_inference_steps: 4,
      },
      {
        headers: {
          Authorization: `Key ${FAL_API_KEY}`,
        },
      }
    );
    
    const taskId = createResponse.data.request_id;
    
    // Ждём результат
    let status = 'IN_PROGRESS';
    let result;
    
    while (status === 'IN_PROGRESS') {
      await new Promise(resolve => setTimeout(resolve, 2000));
      
      const statusResponse = await axios.get(
        `${FAL_API_URL}/requests/${taskId}/status`,
        {
          headers: {
            Authorization: `Key ${FAL_API_KEY}`,
          },
        }
      );
      
      status = statusResponse.data.status;
      if (status === 'COMPLETED') {
        result = statusResponse.data;
      }
    }
    
    return result.images[0].url;
  } catch (error) {
    console.error('Fal.ai error:', error);
    throw error;
  }
}
```

---

## 6. Сравнительная таблица стоимости

| API | Текущая стоимость | Дешёвая альтернатива | Экономия |
|-----|------------------|---------------------|----------|
| Поиск | Perplexity $0.001 | Bing $0.004 | ❌ Дороже |
| Поиск | Perplexity $0.001 | Google Custom Search $0.005 | ❌ Дороже |
| **Поиск** | **Perplexity $0.001** | **Оставить** | ✅ Лучший вариант |
| Промпт | OpenAI $0.01 | Gemini $0.00015 | ✅ **98.5% экономия** |
| Промпт | OpenAI $0.01 | Groq $0.0001 | ✅ **99% экономия** |
| Изображение | Replicate $0.01 | Fal.ai $0.002 | ✅ **80% экономия** |
| Изображение | Replicate $0.01 | Replicate flux-schnell $0.002 | ✅ **80% экономия** |

---

## 7. Рекомендуемая конфигурация

### 🏆 Оптимальный вариант (максимальная экономия):

1. **Поиск:** Оставить **Perplexity** ($0.001) - самый дешёвый и качественный
2. **Промпт:** Переключить на **Google Gemini API** ($0.00015) - экономия 98.5%
3. **Изображение:** Переключить на **Fal.ai** или **Replicate flux-schnell** ($0.002) - экономия 80%

**Новая стоимость для 16000 запросов/день:**
- Поиск: $16/день
- Промпт: $2.4/день
- Изображение: $32/день
- **Итого: ~$50.4/день** (вместо $336/день)

**Экономия: ~$285.6/день = ~$8570/месяц!**

---

## 8. Как получить API ключи

### Google Gemini API:
1. Зайди на https://ai.google.dev/
2. Нажми "Get API Key"
3. Создай проект в Google Cloud
4. Скопируй API ключ

### Bing Web Search API:
1. Зайди на https://www.microsoft.com/en-us/bing/apis/bing-web-search-api
2. Нажми "Get started"
3. Создай ресурс в Azure
4. Скопируй API ключ

### Fal.ai:
1. Зайди на https://fal.ai/
2. Зарегистрируйся
3. Перейди в Settings → API Keys
4. Создай новый ключ

---

## 9. Быстрое переключение

Для быстрого переключения на дешёвые API:

1. **Установи зависимости:**
```bash
npm install @google/generative-ai
# или для Groq
npm install groq-sdk
```

2. **Обнови переменные окружения в Railway:**
```env
# Вместо OPENAI_API_KEY
GEMINI_API_KEY=your_gemini_key

# Вместо REPLICATE_API_KEY (или оставь, но используй flux-schnell)
FAL_API_KEY=your_fal_key
```

3. **Обнови код** (примеры выше)

---

## 10. Итоговые рекомендации

### ✅ Сделать сразу:
1. Переключить генерацию промптов на **Google Gemini API** - экономия 98.5%
2. Переключить генерацию изображений на **Fal.ai** или **Replicate flux-schnell** - экономия 80%
3. Оставить **Perplexity** для поиска - самый дешёвый вариант

### 💰 Результат:
- **Текущая стоимость:** ~$336/день
- **Новая стоимость:** ~$50/день
- **Экономия:** ~$285/день = **~$8550/месяц!**
