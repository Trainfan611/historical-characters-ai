# Быстрая проверка работы Gemini и Nano Banana

## ✅ Проверка переменных окружения

Убедитесь, что в Railway установлены следующие переменные:

```
GEMINI_API_KEY=your_gemini_key_here
NANO_BANANA_API_KEY=your_nano_banana_key_here
```

## 🔍 Проверка работы

### 1. Проверка Gemini (генерация промптов)

После деплоя попробуйте сгенерировать изображение исторической личности. В логах Railway вы должны увидеть:

**Успешная работа:**
```
[Gemini] Starting prompt generation for: [имя]
[Gemini] Prompt generated successfully: [начало промпта]
```

**Если Gemini не работает (fallback на OpenAI):**
```
[Gemini] API not enabled or invalid key, falling back to OpenAI...
[Gemini] Falling back to OpenAI...
```

### 2. Проверка Nano Banana (генерация изображений)

**Успешная работа:**
```
[Nano Banana] Starting image generation with prompt: [начало промпта]
[Nano Banana] Generation succeeded, image URL: [URL изображения]
```

**Если Nano Banana не работает (fallback на Replicate):**
```
[Nano Banana] Error generating image: [ошибка]
[Generate] Nano Banana failed, falling back to Replicate...
[Generate] Fallback to Replicate successful
```

## 🐛 Решение проблем

### Проблема: Gemini возвращает ошибку 403

**Решение:**
1. Включите Generative Language API в Google Cloud Console:
   - https://console.developers.google.com/apis/api/generativelanguage.googleapis.com/overview
   - Нажмите "Enable" (Включить)
   - Подождите несколько минут

2. Проверьте, что ключ правильный и активный

**Временное решение:** Система автоматически переключится на OpenAI для промптов

### Проблема: Nano Banana возвращает SSL ошибку

**Решение:**
1. Проверьте правильность API ключа
2. Проверьте, что URL API правильный (если используете кастомный)
3. Система автоматически попробует альтернативные endpoints

**Временное решение:** Система автоматически переключится на Replicate для изображений

### Проблема: Ошибка "API key is invalid"

**Решение:**
1. Проверьте, что ключи скопированы полностью (без пробелов)
2. Убедитесь, что ключи не истекли
3. Проверьте правильность названий переменных в Railway:
   - `GEMINI_API_KEY` (не `GEMINI_API_KEY_` или другие варианты)
   - `NANO_BANANA_API_KEY` (не `NANO_BANANA_KEY` или другие варианты)

## 📊 Ожидаемое поведение

### Сценарий 1: Все работает идеально
- Gemini генерирует промпт ✅
- Nano Banana генерирует изображение ✅
- Результат сохраняется в БД ✅

### Сценарий 2: Gemini не работает
- OpenAI генерирует промпт (fallback) ✅
- Nano Banana генерирует изображение ✅
- Результат сохраняется в БД ✅

### Сценарий 3: Nano Banana не работает
- Gemini генерирует промпт ✅
- Replicate генерирует изображение (fallback) ✅
- Результат сохраняется в БД ✅

### Сценарий 4: Оба не работают
- OpenAI генерирует промпт (fallback) ✅
- Replicate генерирует изображение (fallback) ✅
- Результат сохраняется в БД ✅

**Важно:** Система всегда будет работать благодаря fallback механизмам!

## 🧪 Тестовый запрос

Попробуйте сгенерировать изображение для:
- "Владимир Путин"
- "Наполеон Бонапарт"
- "Уинстон Черчилль"

Проверьте логи в Railway для диагностики.

## 📝 Логи для проверки

После генерации проверьте логи на наличие:

1. **Для Gemini:**
   - `[Gemini] Starting prompt generation`
   - `[Gemini] Prompt generated successfully` ИЛИ `[Gemini] Falling back to OpenAI`

2. **Для Nano Banana:**
   - `[Nano Banana] Starting image generation`
   - `[Nano Banana] Generation succeeded` ИЛИ `[Generate] Nano Banana failed, falling back to Replicate`

3. **Финальный результат:**
   - `[Generate] Image generated with [сервис]`
   - `[Generate] Generation saved to database`

## ✅ Чеклист готовности

- [ ] `GEMINI_API_KEY` установлен в Railway
- [ ] `NANO_BANANA_API_KEY` установлен в Railway
- [ ] Generative Language API включен в Google Cloud (для Gemini)
- [ ] Приложение перезапущено после добавления переменных
- [ ] Попробована генерация изображения
- [ ] Проверены логи на наличие ошибок

## 🆘 Если ничего не работает

1. Проверьте логи в Railway на наличие ошибок
2. Убедитесь, что переменные окружения правильно названы
3. Проверьте, что ключи не истекли
4. Попробуйте перезапустить приложение в Railway
5. Проверьте, что у вас есть активная подписка (для генерации)

Система имеет fallback механизмы, поэтому должна работать даже при проблемах с одним из сервисов!
