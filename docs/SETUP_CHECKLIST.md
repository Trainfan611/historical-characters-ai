# Чеклист настройки Gemini и Nano Banana

## ✅ Что нужно для работы системы

### 🔑 Обязательные ключи (для fallback)

Эти ключи **ОБЯЗАТЕЛЬНЫ** для работы системы, даже если Gemini и Nano Banana не работают:

1. **OPENAI_API_KEY** ⚠️ ОБЯЗАТЕЛЬНО
   - Для генерации промптов (fallback для Gemini)
   - Получить: https://platform.openai.com/api-keys
   - Без этого ключа система не сможет генерировать промпты

2. **REPLICATE_API_KEY** ⚠️ ОБЯЗАТЕЛЬНО
   - Для генерации изображений (fallback для Nano Banana)
   - Получить: https://replicate.com/account/api-tokens
   - Без этого ключа система не сможет генерировать изображения

### 🎯 Опциональные ключи (для прямого использования)

Эти ключи опциональны, но если они установлены и настроены правильно, система будет использовать их вместо fallback:

3. **GEMINI_API_KEY** (опционально)
   - Для генерации промптов через Gemini
   - Получить: https://ai.google.dev/api?hl=ru
   - **Требует дополнительной настройки** (см. ниже)

4. **NANO_BANANA_API_KEY** (опционально)
   - Для генерации изображений через Nano Banana
   - Получить: https://nanobananaapi.ai/api-key
   - **Требует дополнительной настройки** (см. ниже)

---

## 📋 Чеклист настройки

### Шаг 1: Обязательные ключи (сделать в первую очередь)

- [ ] **OPENAI_API_KEY** установлен в Railway
  - Получить: https://platform.openai.com/api-keys
  - Название переменной: `OPENAI_API_KEY` (точно так!)
  - Формат: `sk-...`

- [ ] **REPLICATE_API_KEY** установлен в Railway
  - Получить: https://replicate.com/account/api-tokens
  - Название переменной: `REPLICATE_API_KEY` (точно так!)
  - Формат: `r8_...`

### Шаг 2: Gemini (опционально, но рекомендуется)

- [ ] **GEMINI_API_KEY** установлен в Railway
  - Получить: https://ai.google.dev/api?hl=ru
  - Название переменной: `GEMINI_API_KEY` (точно так!)

- [ ] **Generative Language API включен в Google Cloud Console**
  - Перейти: https://console.developers.google.com/apis/api/generativelanguage.googleapis.com/overview?project=555550308426
  - Нажать "Enable" (Включить)
  - Подождать 2-5 минут для активации
  - **Без этого Gemini не будет работать!**

### Шаг 3: Nano Banana (опционально, но рекомендуется)

- [ ] **NANO_BANANA_API_KEY** установлен в Railway
  - Получить: https://nanobananaapi.ai/api-key
  - Название переменной: `NANO_BANANA_API_KEY` (точно так!)
  - Убедитесь, что ключ активен

- [ ] **Аккаунт Nano Banana активирован**
  - Проверьте, что аккаунт активен
  - Проверьте баланс кредитов
  - При необходимости пополните баланс

### Шаг 4: Проверка

- [ ] Все переменные установлены в Railway
- [ ] Приложение перезапущено после добавления переменных
- [ ] Попробована генерация изображения
- [ ] Проверены логи на наличие ошибок

---

## 🔍 Как проверить, что все работает

### Проверка в логах Railway:

#### ✅ Если все работает идеально:
```
[Gemini] Prompt generated successfully
[Nano Banana] Task created successfully
[Nano Banana] ✓ Generation succeeded
```

#### ✅ Если Gemini не работает (fallback на OpenAI):
```
[Gemini] ✗ Error generating prompt: status 403
[Gemini] ===== Attempting fallback to OpenAI =====
[Gemini] ✓ OpenAI fallback successful
[Generate] ✓ Prompt generated successfully
```

#### ✅ Если Nano Banana не работает (fallback на Replicate):
```
[Nano Banana] ✗ Error: You do not have access permissions
[Generate] ===== Attempting fallback to Replicate =====
[Generate] ✓ Fallback to Replicate successful!
```

#### ❌ Если что-то не работает:
```
[Generate] ✗ REPLICATE_API_KEY is not set! Cannot use fallback.
```
или
```
[Gemini] ✗ OpenAI fallback also failed: OPENAI_API_KEY is not set
```

---

## 🚨 Минимальные требования

**Для работы системы МИНИМУМ нужны:**

1. ✅ `OPENAI_API_KEY` - для промптов
2. ✅ `REPLICATE_API_KEY` - для изображений

**С этими двумя ключами система будет работать, даже если Gemini и Nano Banana не настроены!**

---

## 📝 Переменные окружения в Railway

Убедитесь, что в Railway установлены следующие переменные:

```
# Обязательные (для fallback)
OPENAI_API_KEY=sk-...
REPLICATE_API_KEY=r8_...

# Опциональные (для прямого использования)
GEMINI_API_KEY=...
NANO_BANANA_API_KEY=...
```

**Важно:**
- Названия переменных должны быть точно такими (без пробелов, без лишних символов)
- Ключи должны быть скопированы полностью
- После добавления переменных перезапустите приложение

---

## 🔧 Решение проблем

### Проблема: Gemini возвращает 403

**Решение:**
1. Включите Generative Language API в Google Cloud Console
2. Подождите 2-5 минут
3. Система автоматически переключится на OpenAI (если `OPENAI_API_KEY` установлен)

### Проблема: Nano Banana возвращает "You do not have access permissions"

**Решение:**
1. Проверьте API ключ на https://nanobananaapi.ai/api-key
2. Убедитесь, что аккаунт активирован
3. Проверьте баланс кредитов
4. Система автоматически переключится на Replicate (если `REPLICATE_API_KEY` установлен)

### Проблема: Fallback не работает

**Решение:**
1. Проверьте наличие `OPENAI_API_KEY` и `REPLICATE_API_KEY`
2. Убедитесь, что ключи не истекли
3. Проверьте правильность названий переменных
4. Перезапустите приложение

---

## ✅ Итоговый чеклист

### Минимальная настройка (система будет работать):
- [ ] `OPENAI_API_KEY` установлен
- [ ] `REPLICATE_API_KEY` установлен
- [ ] Приложение перезапущено

### Полная настройка (использование Gemini и Nano Banana):
- [ ] `OPENAI_API_KEY` установлен ✅
- [ ] `REPLICATE_API_KEY` установлен ✅
- [ ] `GEMINI_API_KEY` установлен
- [ ] Generative Language API включен в Google Cloud
- [ ] `NANO_BANANA_API_KEY` установлен
- [ ] Аккаунт Nano Banana активирован
- [ ] Приложение перезапущено

---

## 🎯 Быстрая проверка

Выполните в терминале Railway или локально:

```bash
# Проверка переменных окружения
echo "OPENAI_API_KEY: ${OPENAI_API_KEY:0:10}..."
echo "REPLICATE_API_KEY: ${REPLICATE_API_KEY:0:10}..."
echo "GEMINI_API_KEY: ${GEMINI_API_KEY:0:10}..."
echo "NANO_BANANA_API_KEY: ${NANO_BANANA_API_KEY:0:10}..."
```

Если переменные не выводятся, они не установлены.

---

## 📞 Полезные ссылки

- **OpenAI API Keys**: https://platform.openai.com/api-keys
- **Replicate API Tokens**: https://replicate.com/account/api-tokens
- **Gemini API**: https://ai.google.dev/api?hl=ru
- **Nano Banana API Key**: https://nanobananaapi.ai/api-key
- **Google Cloud Console**: https://console.developers.google.com/

---

**Помните:** Система всегда должна работать благодаря fallback механизмам, если установлены `OPENAI_API_KEY` и `REPLICATE_API_KEY`!
