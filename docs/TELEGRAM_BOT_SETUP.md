# 🤖 Настройка Telegram бота для доступа к админ-панели

## 📋 Описание

Админ-панель доступна только через Telegram бота. Это обеспечивает безопасность и скрывает админ-панель от обычных пользователей.

## 🚀 Настройка

### 1. Установите переменные окружения

В Railway Variables добавьте:

```
TELEGRAM_ADMIN_ID=ваш_telegram_id
```

Чтобы узнать свой Telegram ID:
- Напишите боту [@userinfobot](https://t.me/userinfobot)
- Или используйте [@getmyid_bot](https://t.me/getmyid_bot)

### 2. Настройте Webhook для бота

Выполните команду (замените `YOUR_BOT_TOKEN` и `YOUR_DOMAIN`):

```bash
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://YOUR_DOMAIN.railway.app/api/telegram/webhook"
```

Или через Railway Shell:

```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook?url=${NEXTAUTH_URL}/api/telegram/webhook"
```

### 3. Проверка

1. Откройте вашего Telegram бота
2. Отправьте команду `/admin`
3. Бот отправит вам секретную ссылку на админ-панель
4. Ссылка действительна 1 час

## 🔐 Безопасность

- Админ-панель недоступна через обычные ссылки
- Доступ только через секретные токены
- Токены действительны 1 час
- Проверка через Telegram ID администратора

## 📱 Использование

### Получить доступ к админ-панели:

1. Откройте Telegram бота
2. Отправьте: `/admin`
3. Получите ссылку вида: `https://ваш-домен.com/admin?token=...`
4. Откройте ссылку в браузере

### Обновить ссылку:

В админ-панели есть кнопка "Получить новую ссылку" для генерации нового токена.

## 🛠️ Устранение проблем

### Бот не отвечает на /admin

1. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен
2. Проверьте, что `TELEGRAM_ADMIN_ID` установлен и правильный
3. Проверьте, что webhook настроен правильно

### Ссылка не работает

1. Убедитесь, что вы вошли в приложение через Telegram
2. Проверьте, что вы назначены админом (`isAdmin = true`)
3. Попросите новую ссылку через бота

### Webhook не работает

Проверьте статус webhook:

```bash
curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
```

---

**Готово! Теперь админ-панель доступна только через Telegram бота.** 🔒
